apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "opc-router.fullname" . }}
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage:
        3Gi